const slider =document.querySelector('.image-column');
const images = document.querySelectorAll('.image-column img');

let counter = 1;
const slideWidth = images[0].clientWidth;

function slide(){
    // slider.computedStyleMap.transition = 
}